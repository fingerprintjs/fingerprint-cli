---
"fingerprint": patch
---

Add commands to create, list, inspect, verify, and delete custom subdomains through the Management API.
Accept hostnames as well as IDs, show DNS setup guidance, and list subdomains when no operation is given.
