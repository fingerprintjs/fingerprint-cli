---
"fingerprint": patch
---

Keep offering server-side verification after the first integration step. The wizard treated the installed server SDK as proof that the Server API was already wired up, so step 2 disappeared from the "What's next?" menu.
