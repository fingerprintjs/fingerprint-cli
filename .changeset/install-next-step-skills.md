---
"fingerprint": minor
---

Install `fingerprint-proxy-integration` and `fingerprint-get-started` into `.claude/skills/` after an integration, so users are pointed at the ad-blocker/subdomain step for their next agent session.
