---
"fingerprint": minor
---

The integration wizard can now set up a custom subdomain: the agent lists, creates and verifies it through Fingerprint tools, and a pending subdomain leaves the step waiting instead of done.
